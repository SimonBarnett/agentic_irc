# Issue #125 evidence (redact host/IP before commit)
#
# Offline (this SHA):
#   py_compile scripts/start_worker_irc_agent.py
#   pytest tests/test_ionos_must5_must2.py tests/test_bobreport.py -k ionos -q
#
# Ionos after merge+pull to C:\ai\agentic_irc (not a side copy) + Watch recycle:
#   ~/.agentic-irc-bobiverse/irc.log or Ergo log — bob-ionos JOIN #bobiverse and JOIN #ionos
#   git worker live — Start-BobWorkerIrcAgent → w-io-<pid> JOIN #ionos (NAMES #ionos)
#
# Example Ergo lines (redacted):
# :bob-ionos!~bob-ionos@<redacted> JOIN #bobiverse
# :bob-ionos!~bob-ionos@<redacted> JOIN #ionos
# :w-io-4412!~w-io-4412@<redacted> JOIN #ionos
#
# Grok-talk MUST 5: bob-peers/ionos.json matches tests/fixtures/ionos-peer-live-2026-09-21.json
# until POINT remaining= or refresh_peer_cursor_remaining; then fuel_ok and ACK omit cannot grok-talk.
