# Issue #125 evidence (redact host/IP before commit)
#
# Offline (replace SHA below after each push):
#   C:\Python312\python.exe -m py_compile scripts/start_worker_irc_agent.py scripts/irc_agent.py
#   C:\Python312\python.exe -m pytest tests/test_ionos_must5_must2.py tests/test_bobreport.py -k ionos -q
#
# MUST 5 replay (live Watch shape, no hand-typed remaining_*):
#   tests/fixtures/ionos-peer-live-2026-09-21.json matches fleet bob-peers/ionos.json
#   fuel via BOB POINT remaining= and/or refresh_peer_cursor_remaining (Get-CursorAgentUsage JSON)
#   irc_agent calls refresh on connect, after local POINT, and before mention ACK
#
# Ionos after merge+pull to C:\ai\agentic_irc (not an untracked side copy) + Watch recycle:
#   ~/.agentic-irc-bobiverse/irc.log — bob-ionos JOIN #bobiverse and JOIN #ionos
#   git worker (cursor-models / grok-build) — sister Start-BobWorkerIrcAgent → w-io-<pid> JOIN #ionos
#
# Example Ergo lines (redacted):
# :bob-ionos!~bob-ionos@<redacted> JOIN #bobiverse
# :bob-ionos!~bob-ionos@<redacted> JOIN #ionos
# :w-io-4412!~w-io-4412@<redacted> JOIN #ionos
#
# Live JOIN evidence is captured on ionos after recycle of the merged SHA (not this dev box).
