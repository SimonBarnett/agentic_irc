# Hostile MRB - agentic_irc moot/file/dumb (2026-09-19)

**Job:** `c312ec14` on ionos  
**Range:** `1a11e98..47931b4`  
**Evidence:** `pytest -q` -> **68 passed, 2 skipped** (re-run on IONOS). Gap doc: `docs/gap-vs-feature-request-2026-09-19.md`.

## Verdict

**FAIL - not ready for human UAT.**

This pass was a real, useful gap-close (docs + thicker section 12 offline tests + small FILE/dumb fixes). It is **not** a complete PDF acceptance. The build agent correctly refused to self-declare UAT-ready. Bob agrees: **do not ship as done.**

Sub-verdict on the *dispatch ticket* (gap doc + strengthen thin tests + honest .NET status): **PASS-with-nits** - that ticket's definition of done was met.

---

## What landed (credit where due)

- Honest gap matrix vs PDF section 12 / Appendix C.
- W1-W6, M1-M7 (incl. 3-nick non-floor via `handle_privmsg`), F2-F8, most of D1-D10 offline.
- FILE DONE path hashes before `complete/`; ABORT; disk-cap refuse.
- Dumb: jail/UNC/secret names, timeout monkeypatch, meta chars, prefix drop.
- Status + README no longer claim "not started"; .NET called a stub; `airc-dumb.cmd` added.
- No Libera from pytest.

---

## Blockers (must fix before Bob says ready for human UAT)

Ordered by PDF severity:

1. **Tier S AIRC-FILE envelope (section 5.2)** - offer still seals raw bytes. Spec is explicit. F1 remains **Partial** until envelope encode/decode + basename jail are real.
2. **`dumb_agent.py` listen loop** - `main` is `INFO no-listen`. PDF Feature C wants a connector process that joins the channel. In-process runner + `dumb_ctl` is not the same deliverable.
3. **D2 on the wire** - unknown operator must not produce a result box; today the IRC path only INFO-logs and does not exercise the full deny-on-wire story the PDF describes for the connector.
4. **Truncated exec spill** - 8 KiB JSON truncate without `dumb/results/<id>.txt` spill when truncated.

**.NET Phase 5** may slip per the PDF ("phases 0-4 even if .NET slips") **only if** the Python connector is actually a listen/join connector. With `no-listen`, slipping .NET does **not** buy a Server 2012 story or a modern dumb daemon. Treat .NET as deferred **after** Python listen works - or explicitly waive Feature C daemon scope in a Simon-signed note (not done).

---

## Nits (non-blocking for a follow-up ticket)

- `YIELD *` sets `floor=None` vs Table 3 prose "returns to chair".
- `FLOOR_IDLE_S` skill-only (PDF allows).
- Skills / `install_skill` not locked by pytest.
- Manual Libera section 12.6 human-only (correct).
- Feature-request md shows encoding mojibake in places (fix if touching the file).

---

## Required fixes for next build agent

1. Implement AIRC-FILE v1 envelope for tier S (encode on offer, decode on receive, basename regex, hash/length gate).
2. Implement `dumb_agent.py` connect/join/flood/CAPA/job loop (stdlib socket+ssl), keep jail/PSK behaviour; offline tests via injected handle_privmsg / fake socket where needed.
3. Prove D2: unknown operator -> no exec + no result ciphertext on the wire (in-process connector path).
4. On truncated stdout/stderr: write `dumb/results/<id>.txt` and set `truncated: true` in JSON.
5. Re-run `pytest -q`; update gap doc; do not claim UAT.

Optional later: Phase 5 .NET protocol clone; `YIELD *` -> chair; skill pytest smoke.

---

## Evidence

| Check | Result |
|---|---|
| `pytest -q` on IONOS post-pull | 68 passed, 2 skipped |
| Diffstat vs pre-dispatch | +595 / -35 across 14 files |
| Libera in CI | Not introduced |
| Self-declared UAT | No |

**Bob:** not ready for human UAT. Re-dispatch with the blocker list above.