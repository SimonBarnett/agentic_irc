# Hostile MRB v2 - agentic_irc moot/file/dumb (2026-09-19)

**Job:** `71588c82` on ionos  
**Range:** `aee2e46..976e4fc`  
**Prior MRB:** [mrb-2026-09-19-v1.md](./mrb-2026-09-19-v1.md) (FAIL - blockers 1-4)  
**Evidence:** `pytest -q` -> **78 passed, 2 skipped** (re-run on IONOS after pull).

## Verdict

**PASS - ready for human UAT** (Python field kit / phases 0-4).

MRB v1 blockers 1-4 are closed with offline evidence. Per the feature PDF, Phase 5 .NET may slip; Python listen is now real. Human UAT is the Libera manual session (`tests/MANUAL.md` / section 12.6) plus any operator check on a real dumb host.

Sub-verdict on the dispatch ticket (blockers 1-4 only): **PASS**.

---

## Blocker close-out

| # | MRB v1 blocker | Status |
|---|---|---|
| 1 | Tier S AIRC-FILE v1 envelope | **Closed.** Encode on offer; decode + basename jail + hash/length gate on receive; F1 tests assert envelope (not raw SEAL). |
| 2 | `dumb_agent.py` listen loop | **Closed.** stdlib socket+ssl connect/join/flood/CAPA/job; `no-listen` gone; offline fake-socket test. |
| 3 | D2 on the wire | **Closed.** `test_d2_unknown_operator_no_result_on_wire`: no `subprocess.run`, no DUMB/PRIVMSG result. |
| 4 | Truncated exec spill | **Closed.** `dumb/results/<id>.txt` + `truncated: true` when over 8 KiB. |

---

## What still is not done (nits / deferred - do not block UAT of Python kit)

1. **Phase 5 .NET protocol clone** - still INFO+exit 0 stub. Allowed to slip after Python listen. Server 2012 story remains open until this lands.
2. **`YIELD *`** - still `floor=None` vs Table 3 "return to chair" prose.
3. **`FLOOR_IDLE_S`** - skill-only (PDF optional).
4. **install_skill / skill text** - not pytest-locked.
5. **AGPK-mode dumb jobs** - connector is PSK default; SEAL-v2 job unwrap on connector not this pass (gap doc note).
6. **Manual Libera** - human-only; that *is* the UAT surface.

---

## Evidence

| Check | Result |
|---|---|
| `pytest -q` on IONOS | 78 passed, 2 skipped |
| Diffstat vs MRB v1 HEAD | +668 / -36 across 10 files |
| Libera in CI | Not introduced |
| Self-declared UAT by build agent | No (correct) |

**Bob:** ready for human UAT on the Python moot/file/dumb field kit. Do not claim Server 2012 / `airc-dumb.exe` until Phase 5. Next optional build: .NET protocol clone only if Simon wants that host story closed.