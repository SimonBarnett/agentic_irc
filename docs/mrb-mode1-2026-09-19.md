# Hostile MRB — Mode 1 agent-to-agent (2026-09-19)

**Session:** Bob (`cm-bob`) ↔ Oscar (`cm-oscar`) on Libera `#cm-bob-oscar`  
**Host:** IONOS `WIN-MPRE8VI4U6U` (AWS box blocked without SASL)  
**Code tip during pass:** `c6e9c53` (NICK_RE hyphen fix)  
**Offline suite:** `pytest -q` → **78 passed, 2 skipped** (re-run on IONOS).

## Verdict

**PASS — mode-1 agent-to-agent proven on IONOS** (connect + mutual AGPK + SEAL secret + FILE tier S).

**Not** a full PDF Appendix C / manual Libera checklist close-out. AWS→Libera without NickServ SASL remains blocked. .NET Phase 5 still deferred (unchanged from MRB v2).

---

## Evidence (live)

| Step | Result |
|---|---|
| TLS join (IONOS) | Both nicks `INFO joined #cm-bob-oscar` |
| Mutual AGPK | Bob pinned oscar; oscar `peers.json` has Bob `ElGfrGcx…Dv0o=` |
| Cleartext outbox | `CLEARTEXT_PING_FROM_BOB` seen on oscar `irc.log` |
| SEAL bob→oscar | `INFO SEAL 1e2eff866b63b790 -> inbox (18 bytes)`; plaintext `MODE1_SECRET_OK7` |
| FILE tier S | `INFO file OFFER id=6e4a50e9098f56a5` then `INFO file DONE id=6e4a50e9098f56a5 ok`; file at `files/complete/6e4a50e9098f56a5-mode1-file.txt` (41 bytes) |
| Clients still up | `cm-bob` + `cm-oscar` python processes on IONOS after test |

---

## Bugs found in this UAT (and status)

1. **CAP END missing when SASL unset** — no `001` registration. **Fixed** `8c168c9`.
2. **`NICK_RE` rejected hyphenated nicks** (`cm-oscar`) — `parse_seal_line` returned `None`, SEAL silently ignored on wire. Cause: `-` inside a character class formed a range `\-_`. **Fixed** `c6e9c53` (hyphen at end of class).
3. **TOFU first-pin footgun** — bob briefly re-announced oscar's AGPK as "bob"; oscar pinned wrong key; later correct AGPK → mismatch ignored. **Operational** — wipe `peers.json` / restart receiver; do not announce wrong AGPK.
4. **AWS / Grok Bot box** — Libera requires SASL + verified NickServ email. Mode-1 must run from IONOS (or with SASL) until accounts exist.
5. **Windows `Start-Process` hello quoting** — `--hello "oscar online mode1"` can split into unrecognized args. Use single-token hello or argv array carefully.

---

## Required follow-up for build agent (hand-off)

1. Add **offline pytest** that `NICK_RE` matches hyphenated nicks (`cm-oscar`, `cm-bob`) and that `parse_seal_line` accepts a SEAL v2 line with those nicks.
2. Add offline SEAL seal→parse→`FragmentStore`→`open_bytes_v2` roundtrip using hyphenated nicks (no Libera).
3. Document in `README.md` / skill: AWS Libera SASL gate; prefer IONOS for unattended mode-1; TOFU wipe procedure if wrong first pin.
4. Optional: regression that CAP END is sent on `sasl_plain()` when env unset (unit or small harness).
5. Do **not** open Libera from CI. Do **not** claim Server 2012 / .NET done.

---

## Pass bar for this MRB

- Live mode-1 SEAL + FILE on a private channel between two agent homes — **met**.
- Hyphen nick SEAL parse — **met after fix** (needs pytest lock).
- Full manual `tests/MANUAL.md` moot + dumb + Libera matrix — **not claimed**.

**Bob:** mode-1 PASS on IONOS. Hand remaining pytest/docs lock to build agent.