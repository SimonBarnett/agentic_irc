# Hostile MRB — Mode 2 moot + SEAL/FILE (2026-09-19)

**Session:** Bob (`cm-bob` chair) + Oscar (`cm-oscar`) + Spider (`cm-spider`) on `#cm-bob-oscar`  
**Host:** IONOS  
**Moot id:** `47e6b0ca63b9b2dd`  
**Code tip:** `70f85ab`  
**Offline suite:** 81 passed, 2 skipped (unchanged this pass).

## Verdict

**PASS-with-nits** for live mode-2 field exercise (3-nick moot + multi-party SEAL + FILE tier S).

**Not** ready to claim moot state-machine complete: chair roster after JOIN stayed `{cm-bob}` only. Hand remaining bugs to a build agent.

---

## Evidence (live)

### Moot
| Step | Result |
|---|---|
| OPEN floor topic `mode2 SEAL+FILE probe` | Seen: oscar/spider `INFO moot OPEN id=47e6b0ca63b9b2dd chair=cm-bob` |
| JOIN oscar/spider | CLI join sent; participant transcripts show SAY |
| FLOOR → oscar SAY / YIELD; FLOOR → spider SAY / YIELD | Oscar/spider transcripts include SAY lines |
| CLOSE | `cm-bob CLOSE mode2 moot exercised` |
| Chair `moot/*.json` roster | **Bug:** `roster: ["cm-bob"]` only after close; `floor: null`, state closed |

### SEAL
| Path | Result |
|---|---|
| bob → oscar | inbox `e6566aa0…` plaintext `MODE2_SECRET_BOB_TO_OSCAR` |
| bob → spider | inbox `c754b400…` plaintext `MODE2_SECRET_BOB_TO_SPIDER` |
| oscar → spider | inbox `2a78f8bd…` plaintext `MODE2_SECRET_OSCAR_TO_SPIDER` |

### FILE tier S
| Path | Result |
|---|---|
| oscar → spider `mode2-file-b.txt` | `INFO file DONE id=63ea1e9d053ed590 ok` |
| bob → oscar first `mode2-file-a.txt` | OFFER seen; **no DONE** |
| bob → oscar retry `mode2-file-a2.txt` | `INFO file DONE id=67bcd11a32cba7fb ok` |

---

## Blockers / required fixes for build agent

1. **Moot roster on chair** — after JOINA from other nicks, chair `moot/<id>.json` roster must include joiners (and seq/floor transitions). Today only chair remains on roster.
2. **Chair transcript completeness** — bob `moot/<id>.txt` nearly empty vs oscar/spider transcripts; wire APPLY should append OPEN/JOIN/FLOOR/SAY/YIELD/CLOSE on all participants including chair.
3. **FILE tier S flake** — first bob→oscar OFFER did not complete; retry succeeded. Find race (ACCEPT not sent, DONE before bag ready, outbox flood).
4. Add offline tests locking (1)–(2) without Libera.

## Nits

- Duplicate CLOSE lines in spider transcript.
- `YIELD *` vs chair prose still open from earlier gap list.
- Agent INFO does not always log JOIN/FLOOR verbs (only OPEN).

## Pass bar met

- Three agents on one private channel with TOFU pins.
- Floor moot OPEN → SAY under floor → CLOSE.
- SEAL among all three directed pairs tested.
- FILE tier S works at least oscar→spider and bob→oscar (retry).

**Bob:** mode-2 PASS-with-nits. Queue build agent for roster/transcript/FILE flake.