# Hostile MRB - Mode 3 thin moot CLI (2026-09-19)

**Repo:** SimonBarnett/agentic_irc  
**Commit:** `bd789b8` feat: Mode 3 thin moot CLI P0-P3 (Win32 ANSI, not Win95 TLS)  
**Job:** ionos `9f96bc0e-7517-4f01-98fc-ba792a1b28b1`  
**Spec:** docs/feature-request-mode3-win95-thin-moot-cli-2026-09-19.md + docs/build-and-test-plan-mode3.md

## Verdict

**PASS-with-nits** for the **first ticket** (P0-P3).  
**Not ready for human UAT.** A5 live IONOS moot+exec smoke is still open.

Honest about Win95: U1 closed as unclaimed; live Libera floor documented as Win8 / Server 2012+. That matches the plan defaults. It does **not** satisfy a literal "runs on Windows 95" reading of the original ask - and the docs correctly refuse to fake that.

## Evidence

| Gate | Result |
|---|---|
| A1 Release binary | **Pass.** Releases `mode3-thin` and `airc-moot-thin-v0.1.0` published by Actions. |
| A2 Rebuild updates release | **Pass.** Workflow `mode3-thin-release` run 1 conclusion **success** on `bd789b8`. |
| A3 Offline tests | **Pass.** `pytest -q` -> **103 passed, 2 skipped**. Local `airc-moot-thin.exe --selftest` ok (jail, operators, AES-GCM vector, exec, truncation, moot JOIN). Offline `--job-in` ping -> `ok:true`. |
| A4 OS matrix | **Pass.** docs/mode3-os-matrix.md + mode3-tls-spike.md; Win95/98/NT4/XP live rows blocked. |
| A5 IONOS live smoke | **Fail / not run.** Matrix and build agent both say Phase 4 not done. No hostname-to-cm-bob evidence. |
| A6 Bob UAT phrase | **Correctly withheld.** |
| PE shape | i386 (`0x14c`), PE32, subsystem CONSOLE (3). Exe **not** tracked in git (gitignore). |
| CI test workflow | **success** on same SHA. |

Release URLs:
- https://github.com/SimonBarnett/agentic_irc/releases/tag/mode3-thin
- https://github.com/SimonBarnett/agentic_irc/releases/tag/airc-moot-thin-v0.1.0

Actions:
- https://github.com/SimonBarnett/agentic_irc/actions/runs/35461747146 (release)
- https://github.com/SimonBarnett/agentic_irc/actions/runs/35461747150 (test)

## What landed (credit only where evidenced)

- P0 docs: TLS spike, release policy, OS matrix.
- P1 fixtures + `tests/test_moot_thin_proto.py`.
- P2 Win32 ANSI sources under `src/moot_thin/` (Schannel, DUMB v1 jobs, jail, `--operators`, `--selftest` / `--offline`).
- P3 `.github/workflows/mode3-thin-release.yml` rolling + immutable tags.
- Defaults held: Win32 ANSI (not 16-bit DOS), DUMB jobs + moot JOIN, operators required, no Win95 TLS claim, Mode 1/2 Python left alone, .NET Phase 5 untouched.

## Non-conformances / required follow-ups

### NC-01 (blocker for human UAT) - A5 live smoke missing

Run on IONOS: chair `cm-bob` OPEN moot; start release `airc-moot-thin.exe` with real key + operators; sealed `exec hostname` (or equivalent); stdout visible to chair. Paste log excerpt into `/docs`. Until then **do not** say ready for human UAT.

### NC-02 (nit) - Original "Win95" ask remains unsatisfied

Product is XP-era compile macros + Win8+ live TLS. Acceptable for P0 plan defaults; if Simon still wants a true 9x PE, that is a **new** spike (OpenWatcom + static TLS or relay), not a silent claim on this artefact.

### NC-03 (nit) - `moot.c` is minimal

JOIN-focused stub is fine for thin client; confirm live smoke exercises roster visibility from Python chair. Expand only if smoke shows missing verbs.

### NC-04 (nit) - Local untracked `tools/` on ionos clone

Working tree showed `?? tools/` after pull - confirm it is not a forgotten Mode-3 deliverable; do not commit junk.

## Pass bar for next MRB (UAT-ready)

- NC-01 live smoke evidence in `/docs`
- No secret leaks in release assets (already clean)
- Selftest + pytest still green
- Bob may then say **ready for human UAT** of Mode 3 on Win8+ only

## Hand-off

Optional build agent: Phase 4 A5 IONOS live smoke + doc only. Do not re-litigate P0-P3 unless smoke finds bugs.