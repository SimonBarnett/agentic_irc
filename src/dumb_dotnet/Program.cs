using System;
namespace AircDumb
{
    internal static class Program
    {
        private static int Main(string[] args)
        {
            Console.WriteLine("INFO airc-dumb net45 stub; protocol clone of scripts/dumb_agent.py");
            Console.WriteLine("INFO TLS 1.2 required on Server 2012 (SchUseStrongCrypto)");
            return 0;
        }
    }
}
